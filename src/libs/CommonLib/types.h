#pragma once

using uchar  = unsigned char;
using ushort = unsigned short;
using ulong  = unsigned long;
using uint   = unsigned int;
using int64  = __int64;
using uint64 = unsigned __int64;
using real_t = double;

enum class EYE : int { UNKNOWN, OD = 1, OS = 2 };
